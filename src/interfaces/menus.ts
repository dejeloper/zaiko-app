export interface IMenuGeneral {
  id: number;
  name: string;
  descriptionMenu: string;
  url: string;
  image?: string;
}

export interface IMenuNav {
  id: number;
  name: string;
  descriptionMenu: string;
  url: string;
  icon: string;
}
